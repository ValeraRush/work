//В функцию передаем массив и значение. По результату выполнения вывести все номер элементов массива, которые меньше второго параметра.
let array = [2,4,5,6,7,8,8,8,7,7,8,9,6,5,7];
testFunction=(arr,elem)=>{
	return array.reduce(function(accum,current,index) {
		if (current<elem){
		accum.push(current)
		}
	return accum
	},[]);

	};

console.log(testFunction(array,9));
